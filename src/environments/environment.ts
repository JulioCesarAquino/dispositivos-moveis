// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyAPvr5C4U8tZkNoMhYo95lMTCTbwFAAs7A',
    authDomain: 'matik-bia.firebaseapp.com',
    databaseURL: 'https://matik-bia.firebaseio.com',
    projectId: 'matik-bia',
    storageBucket: 'matik-bia.appspot.com',
    messagingSenderId: '290008203798',
    appId: '1:290008203798:web:ddd724aaf431832ef9eb37'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
